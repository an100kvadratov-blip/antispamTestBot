# 🛡️ Telegram Антиспам Бот

Бот для автоматического удаления спама в комментариях Telegram канала.

## 🌟 Возможности

- ✅ Удаление сообщений со стоп-словами (221 паттерн)
- ✅ Блокировка ссылок и упоминаний
- ✅ Защита владельца (его сообщения не удаляются)
- ✅ Официальные посты канала не проверяются
- ✅ Детальное логирование
- ✅ Статистика работы
- ✅ Уведомления в ЛС (опционально)

## 🚀 Быстрый старт (локально)

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/antispam-bot.git
cd antispam-bot
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv .venv
source .venv/bin/activate  # MacOS/Linux
# или .venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте `.env` файл:
```bash
cp .env.example .env
nano .env  # Заполните своими данными
```

5. Запустите бота:
```bash
python antispamTestBot_polling.py
```

## 📦 Деплой на сервер

Используйте `antispamTestBot_webhook.py` для продакшена.

Подробные инструкции в `DEPLOY_GUIDE.md`

## ⚙️ Настройка

### Переменные окружения (.env):

| Переменная | Обязательно | Описание |
|-----------|-------------|----------|
| TOKEN | ✅ | Токен бота от @BotFather |
| OWNER_ID | ✅ | Ваш Telegram ID |
| PROTECTED_CHANNEL_ID | ✅ | ID группы обсуждений |
| WEBHOOK_URL | ⬜ | URL для webhook (продакшн) |
| NOTIFY_OWNER | ⬜ | Уведомления в ЛС (true/false) |
| LOG_LEVEL | ⬜ | DEBUG, INFO, WARNING, ERROR |

### Добавление стоп-слов:

Редактируйте `stopwords.txt`:
```bash
nano stopwords.txt
```

Перезагрузите без перезапуска:
```bash
curl -X POST http://localhost:8080/reload
```

## 📊 API Endpoints

- `GET /health` - Проверка работы
- `GET /stats` - Статистика
- `POST /reload` - Перезагрузка стоп-слов

## 📝 Логи

Логи сохраняются в `antispam_bot.log` с ротацией (max 10MB, 5 файлов)

## 🤝 Вклад

Pull requests приветствуются!

## 📄 Лицензия

MIT
